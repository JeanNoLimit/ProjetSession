{% extends 'base.html.twig' %}

{% block title %}Liste des stagiaires{% endblock %}

{% block body %}
    {% for message in app.flashes('success') %}
                
    <div class="flash flash-success">
        {{ message }}
    </div>

    {% endfor %}
    <h1>Liste des stagiaires</h1>

    <a href="{{path('add_stagiaire')}}" class="btn">Nouveau Stagiaire</a>

    <table class="tList">
    <thead>
        <tr>
            <th>Prénom Nom</th>
            <th>modifications</th>
        </tr>
    </thead>
    <tbody>
        {% for stagiaire in stagiaires %} 
    
        <tr>
            <td><a href="{{path('show_stagiaire', {'id': stagiaire.id})}}">{{stagiaire.prenom}} {{stagiaire.nom}}</a></td>
            <td><a href="{{path('edit_stagiaire', {'id': stagiaire.id})}}"><i class="fa-regular fa-pen-to-square"></i></a> / <a href="{{path('delete_stagiaire', {'id': stagiaire.id})}}"><i class="fa-regular fa-trash-can"></i></a></td>
        </tr> 

        {% endfor %}
    </tbody>
    </table>
{% endblock %}
